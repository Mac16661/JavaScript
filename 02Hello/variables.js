var fullName = "Subhodip Ghosh" // (var has gobal scope)

//how to name variable:

//fullName (camel casing)
//full-name (- style)
//full_name (snake case)
//FullName (this is not a good practice).

var courseName ="React JS Bootcamp"

var isLoggedIn = false

var loggedCount = 34;

var paymentMethod;

paymentMethod = "Credit Card"

console.log(paymentMethod);